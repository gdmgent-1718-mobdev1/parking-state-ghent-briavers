function garage(name, totalCapacity, availableCapacity, adress, open){
    this.name = name;
    this.totalSpace = totalCapacity;
    this.ocupiedSpace = availableCapacity;
    this.adress = adress;
    this.open =open;

}